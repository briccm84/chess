package chess;

public class Game {

    public Game(){

    }

    public boolean gameLoop(){
        boolean running = true;
         while(running){
             /*
             whitesTurn();
             running = !checkForEnd();
             blacksTurn();
             running = !checkForEnd();
              */
         }
         return true;
    }
}
